# WARNING WARNING WARNING WARNING WARNING
# DO NOT MODIFY THIS FILE!! This file is generated!
# WARNING WARNING WARNING WARNING WARNING

import logging
import std.xml as xml
import stratoweave.ttt as ttt
import yang.adata as yadata
import yang.gdata as ygdata
import yang.xml as yxml

from fleetmgr.layers.y_0 import maintenance__schedules
from fleetmgr.layers.y_0 import fleetmgr__fleet__node_entry
from fleetmgr.layers.y_0 import fleetmgr__fleet__device_entry
from fleetmgr.layers.y_0 import software__software__upgrade_campaign_entry
from fleetmgr.layers.y_0 import software__software__upgrade_campaign__dynstate
from fleetmgr.layers.y_0_oper import software__software__upgrade_campaign_entry as software__software__upgrade_campaign_entry__oper
from fleetmgr.layers.y_0 import SRC_DNODE
from fleetmgr.layers.y_1_loose import root as output_root

def o_root():
    return output_root()


class Schedules(ttt.TransformFunction):
    transform: mut(maintenance__schedules) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return maintenance__schedules

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = maintenance__schedules.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['maintenance:schedules'])
        modeled_input = maintenance__schedules.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class Node(ttt.TransformFunction):
    transform: mut(fleetmgr__fleet__node_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return fleetmgr__fleet__node_entry

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = fleetmgr__fleet__node_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['fleetmgr:fleet', 'node'])
        modeled_input = fleetmgr__fleet__node_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class Device(ttt.TransformFunction):
    transform: mut(fleetmgr__fleet__device_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return fleetmgr__fleet__device_entry

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = fleetmgr__fleet__device_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['fleetmgr:fleet', 'device'])
        modeled_input = fleetmgr__fleet__device_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class UpgradeCampaign(ttt.TransformFunction):
    transform: mut(software__software__upgrade_campaign_entry, linked: ygdata.Node, dynstate: ?software__software__upgrade_campaign__dynstate) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return software__software__upgrade_campaign_entry

    @staticmethod
    def state_type():
        """Return the modeled operational state type for this transform"""
        return software__software__upgrade_campaign_entry__oper

    @staticmethod
    def new_state(path: ttt.Path):
        """Create modeled operational state for this transform path"""
        return software__software__upgrade_campaign_entry__oper.from_gdata(ttt.path_key_data(path))

    @staticmethod
    def dynstate_type():
        """Return the modeled dynstate type for this transform"""
        return software__software__upgrade_campaign__dynstate

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = software__software__upgrade_campaign_entry.from_gdata(i)
        modeled_dynstate = software__software__upgrade_campaign__dynstate.from_gdata(dynstate) if dynstate is not None else None

        return self.transform(modeled_input, linked, modeled_dynstate).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['software:software', 'upgrade-campaign'])
        modeled_input = software__software__upgrade_campaign_entry.from_gdata(gdata_input)
        modeled_dynstate = software__software__upgrade_campaign__dynstate.from_gdata(dynstate) if dynstate is not None else None

        return self.transform(modeled_input, linked, modeled_dynstate).to_gdata(), None
