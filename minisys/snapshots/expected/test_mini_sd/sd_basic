# === Layer 0 adata ===
# Top node: /
l0 = root()

# Container: /netinfra/global-settings
l0.netinfra.global_settings.asn = u64(123)

# List /netinfra/router element: rtr1
router_element = l0.netinfra.router.create('rtr1', id=u64(1), type='ietf', mock=True)

# List /netinfra/router element: rtr2
router_element = l0.netinfra.router.create('rtr2', id=u64(2), type='ietf', mock=True)

# === Round-trip diff (per device) ===
# device rtr1
no diff

# device rtr2
no diff